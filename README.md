# Governed AI Course Generator — Spec v2

Published page: `index.html` — a self-contained specification of a governed AI course
generator for an LXP platform (formal verification layers, managed moderation and
retrieval, human-in-the-loop publication).

Written as the second version of a final-project specification for the AI Architect course.

## What's here

| File | Purpose |
|---|---|
| `index.html` | the page itself — all styling inline, diagrams rendered by Mermaid from a CDN |
| `.nojekyll` | tells GitHub Pages to serve the files as-is, without Jekyll processing |

## Local preview

Open `index.html` in a browser — no build step, no dependencies.
